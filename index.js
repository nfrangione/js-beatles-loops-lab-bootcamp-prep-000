// add solution here

function theBeatlesPlay(musicians, instruments) {
  var musicianInstrument = [];
  for (let i = 0; i < musicians.length; i++) {
    musicianInstrument[i] = `${musicians[i]} plays ${instrument[i]}`
  }
  return musicianInstrument;
}
